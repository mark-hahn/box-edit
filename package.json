{
  "name": "box-select",
  "main": "./lib/box-select",
  "version": "0.1.0",
  "description": "An Atom package to allow rectangular selection with support for short line lengths.",
  "repository": "https://github.com/mark-hahn/box-select",
  "license": "MIT",
  "engines": {
    "atom": ">1.0.0"
  },
  "dependencies": {
    "sub-atom": "*",
  }
}